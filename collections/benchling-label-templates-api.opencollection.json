{
  "opencollection": "1.0.0",
  "info": {
    "name": "Benchling AA Sequences Label Templates API",
    "version": "2.0.0"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "client_credentials",
      "accessTokenUrl": "/api/v2/token",
      "credentials": {
        "clientId": "{{clientId}}",
        "clientSecret": "{{clientSecret}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Label Templates",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "List label templates",
            "type": "http"
          },
          "http": {
            "method": "GET",
            "url": "/api/v2/registries/:registry_id/label-templates",
            "params": [
              {
                "name": "registry_id",
                "value": "",
                "type": "path",
                "description": "ID of the registry to list the label templates of."
              },
              {
                "name": "name",
                "value": "",
                "type": "query",
                "description": "Name of a label template. Restricts results to those with the specified name."
              }
            ]
          },
          "docs": "List label templates"
        }
      ]
    }
  ],
  "bundled": true
}